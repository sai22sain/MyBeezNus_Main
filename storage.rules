rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Each user owns their folder: users/{uid}/**
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
